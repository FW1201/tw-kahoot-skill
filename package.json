{
  "name": "tw-kahoot-skill",
  "version": "0.1.0",
  "description": "Agent skill: build a Kahoot official spreadsheet-import file with an import-readiness report for Taiwanese K-12 teachers. No browser automation, no API — spreadsheet importer only.",
  "type": "module",
  "license": "MIT",
  "private": true,
  "scripts": {
    "test": "node --test tests/*.test.mjs"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "exceljs": "4.4.0",
    "zod": "3.25.76"
  }
}
